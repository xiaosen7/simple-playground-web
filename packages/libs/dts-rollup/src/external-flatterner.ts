interface IExternalFlattenerOptions {}

/**
 * Input *.ts entry files, collect all *.d.ts files which are in node_modules
 */
export class ExternalFlattener {
  constructor(options: IExternalFlattenerOptions) {}
}
